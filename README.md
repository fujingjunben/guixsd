# How to install GuixSD in VirtualBox
first, use *VirtualBox* command tool convert guix usb raw iamge to vdi or vmdk like this:
``` shell
VBoxManage convertfromraw your-raw-image your-vdi.vdi --format vdi
```
